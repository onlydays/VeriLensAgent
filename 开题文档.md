# VeriLens —— 带评测与可观测性的技术调研 Agent（开题文档）

> 一个技术调研 Agent，但**主角不是 Agent，而是给它配套的评测 + 可观测性体系**。
> 定位一句话：**不做「又一个 Agent」，做「一个能被证明可靠的 Agent」——用自研的评测工具，把它的可靠性量化成数字。**

---

## 一、项目定位（与「普通 Agent 项目」的关键区别）

| | 普通 Agent 项目（2025 版） | 本项目（2026 版） |
|---|---|---|
| 主角 | 会写报告的 Agent | **评测工具 + 可观测性体系** |
| 亮点 | 「我调 API 做了个 Agent」 | 「我给 Agent 建了评测系统，准确率从 X% 提到 Y%」 |
| 面试官怎么看 | 又一个套壳 demo | 你懂 LLM 的不可靠，并有工程能力治理它 |

核心思路：**Agent 只是「被评测的对象」，评测工具要能独立复用到任何同类 Agent 上。** 两者解耦，是技术深度的来源。

## 二、核心亮点（可量化的那行字）

- 自建评测集 + 独立可复用的评测工具（eval harness），量化 Agent 的「引用准确率 / 事实正确率 / 覆盖度 / 忠实度」。
- 每步 trace 的可观测性体系（推理、工具调用、token、耗时、失败点），出了问题能定位到具体步骤。
- 用评测结果反向迭代 Agent，产出「引用准确率从 X% 提到 Y%」的量化结论（**数字以你的真实评测为准，不预设、不编造**）。

## 三、它做什么、谁会用（垂直场景：技术调研）

**场景**：输入一个技术选型 / 调研问题（如"帮我调研 Go 和 Rust 在云原生方向的现状对比"），Agent 自主规划检索 → 搜索 → 抓取 → 交叉验证 → 输出带引用的调研报告 + 选型建议。

**谁会用**：

| 用户 | 场景 |
|---|---|
| 求职者 / 学生（你自己就是第一用户） | 技术选型、行业/公司调研 |
| 开发者 | 陌生技术快速上手、技术选型对比 |
| 产品经理 / 分析师 | 竞品分析、需要引用来源的调研报告 |

**为什么选「技术调研」这个垂直场景**：它天然**可评测**——「引用是否支持结论」「事实是否正确」能客观核对，正好喂给评测工具。这是选场景的第一原则。

---

## 四、技术栈

| 层 | 选型 |
|---|---|
| 语言 | Python 3.11+ |
| LLM | Anthropic Claude API（tool_use），可替换 OpenAI 兼容接口 |
| 搜索 | Tavily（免费额度）→ 免费兜底 DuckDuckGo（`ddgs`） |
| 抓取 | `trafilatura`（正文提取）+ `requests` |
| 判定 | LLM-as-judge + 规则双通道，人工抽样校准 |
| 可观测性 | `logging` + 每步 trace 落盘 JSONL |
| 依赖管理 | `uv` 或 `pip + venv` |

## 五、系统架构（三层，评测层是主角）

```
                     ┌─────────────────────────────────┐
   问题  ──────────▶ │  Agent 层（被评测对象）           │
                     │  ReAct 循环 / 工具 / 上下文 / 报告 │
                     └───────────────┬─────────────────┘
                                     │ 产出 Report（结论+引用）
                                     ▼
        ┌────────────────────────────────────────────┐
        │  评测层（独立可复用，主角）                   │
        │  AgentProtocol → EvalHarness → 指标打分      │
        │  （引用准确率 / 事实正确率 / 覆盖度 / 忠实度）  │
        └───────────────┬────────────────────────────┘
                        │ 量化结果 + 失败定位
                        ▼
        ┌────────────────────────────────────────────┐
        │  可观测性层                                  │
        │  每步 trace（推理/工具/输入输出/token/耗时）   │
        └────────────────────────────────────────────┘
```

**评测与可观测性的分工**（面试必讲）：
- **评测 = 结果层**：衡量「结果好不好」（引用准不准、事实对不对）。
- **可观测性 = 过程层**：观察「过程发生了什么」（哪一步慢、哪一步失败、token 在哪烧掉）。

## 六、核心接口签名（体现「评测工具可复用」）

```python
# 关键抽象：任何 Agent 实现这个协议，就能被评测工具评测 —— eval/protocol.py
@dataclass
class Report:
    conclusion: str                    # 最终结论
    claims: list["Claim"]              # 结论拆成的断言
@dataclass
class Claim:
    text: str
    refs: list[str]                    # 支持该断言的来源 URL

class AgentProtocol(Protocol):
    def research(self, question: str) -> Report: ...

# 评测工具（独立于 Agent，可复用）—— eval/harness.py
@dataclass
class EvalCase:
    question: str
    key_facts: list[str]               # 标准事实点（人工标注）
    refs: list[str]                    # 应引用的权威来源

class EvalHarness:
    def __init__(self, dataset, metrics): ...
    def run(self, agent: AgentProtocol) -> EvalReport: ...      # 跑评测
    def compare(self, before: EvalReport, after: EvalReport): ... # 回归对比

# 指标 —— eval/metrics.py
class Metric(ABC):
    def score(self, report: Report, case: EvalCase) -> float: ...
class CitationAccuracy(Metric): ...   # 引用准确率
class Factuality(Metric): ...         # 事实正确率
class Coverage(Metric): ...           # 覆盖度
class Faithfulness(Metric): ...       # 忠实度（反幻觉）

# Agent 侧（被评测对象）—— src/agent/loop.py
class ResearchAgent(AgentProtocol):
    def research(self, question: str) -> Report: ...
    def _step(self, history) -> ToolCall | FinalAnswer: ...
    def _execute(self, tool_call) -> str: ...
```

> 设计要点：`EvalHarness` 只依赖 `AgentProtocol`，不依赖具体 Agent 内部实现。这意味着你的评测工具能拿去评测**任何**同类 Agent——这是「独立可复用」的落点，也是面试深挖的核心。

## 七、评测指标定义（要能落地）

| 指标 | 定义 | 算法 |
|---|---|---|
| **引用准确率** | 有引用、且引用内容确实支持该断言的比例 | 有支持引用的断言数 / 总断言数 |
| **事实正确率** | 断言与标准事实点一致的比例 | 一致断言数 / 总断言数 |
| **覆盖度** | 标准事实点被报告覆盖的比例 | 覆盖的关键事实数 / 总关键事实数 |
| **忠实度** | 断言忠于引用来源、未编造的比例 | 规则 + LLM-as-judge 判定 |

判定通道：先规则（引用 URL 是否有效、是否命中来源），再 LLM-as-judge（引用是否支持断言），最后**人工抽样校准**（避免 LLM 判 LLM 的偏差）。

## 八、评测集构建（面试必问，提前想清）

1. 选 20—30 个有明确答案的技术选型/调研问题（答案相对稳定、可核对）。
2. 每个问题人工标注：`key_facts`（3—5 个关键事实点）+ `refs`（应引用的权威来源，如官方文档、权威对比文章）。
3. 保证：**评测集与训练/调 prompt 的过程隔离**（防过拟合），可复现、可版本化。

## 九、目录结构

```
verilens/
├── README.md
├── requirements.txt
├── src/
│   ├── agent/               # Agent 层（被评测对象）
│   │   ├── loop.py          # ReAct 主循环
│   │   ├── llm.py           # LLM 调用封装（tool_use）
│   │   ├── context.py       # 上下文 / token 管理
│   │   ├── report.py        # 报告生成
│   │   └── tools/
│   │       ├── base.py      # Tool 抽象
│   │       ├── search.py    # 搜索
│   │       └── fetch.py     # 网页抓取
│   ├── eval/                # 评测层（独立可复用，主角）
│   │   ├── protocol.py      # AgentProtocol / Report / Claim
│   │   ├── harness.py       # EvalHarness 评测流程
│   │   ├── metrics.py       # 四个指标
│   │   ├── judge.py         # 规则 + LLM-as-judge 判定
│   │   └── dataset.py       # 评测集加载
│   └── observability/       # 可观测性层
│       ├── tracer.py        # 每步 trace 落盘
│       └── analyzer.py      # 失败 / 耗时 / token 分析
├── datasets/                # 评测集（问题 + 标准事实 + 来源）
├── logs/                    # trace（JSONL）
└── tests/
```

## 十、里程碑与验收标准（4—6 周）

### W1 —— Agent 最小闭环（先有被评测对象）
- 手写 ReAct 循环 + 1 个搜索工具，LLM API 跑通。
- 验收：`research("帮我调研 X")` 能跑通单步，返回一段带引用的小结。

### W2 —— Agent 完善
- 加抓取工具；上下文/token 管理；工具失败重试/超时/JSON 自愈；循环检测。
- 验收：能对一个问题自主完成「搜索→抓取→再搜索」多轮，不因单次失败崩。

### W3 —— 评测工具（项目真正的核心）
- 实现 `AgentProtocol`、`EvalHarness`、四个指标、判定通道；建 20 条评测集。
- 验收：`EvalHarness.run(agent)` 能对任意实现协议的对象跑出四个指标分。

### W4 —— 量化 + 迭代 + 可观测性
- 加每步 trace；用评测结果定位 Agent 短板（如哪类问题引用差），针对性改进。
- 验收：跑出「改进前 vs 改进后」两组真实指标，形成「X% → Y%」的量化结论。

### W5—6（可选，加分）
- 回归对比自动化；trace 可视化；README + demo 录屏；换一个 Agent 验证评测工具的通用性。

## 十一、面试深挖点（提前准备）

1. 你的评测工具怎么做到「独立可复用」？凭什么能评测别的 Agent？
2. 四个指标分别怎么定义、怎么算？为什么选这四个？
3. 引用准确率怎么判定「引用确实支持断言」？LLM-as-judge 怎么避免偏差？
4. 评测集怎么建？怎么保证评测客观、没被 Agent 过拟合？
5. 评测（结果层）和可观测性（过程层）有什么区别？分别解决什么？
6. 用评测结果改进 Agent 时，具体改了什么、为什么指标涨了？
7. 为什么手写 ReAct，不用 LangChain / LangGraph？

## 十二、风险与边界（别过度承诺）

- 本项目是**个人学习 / 求职项目**：目标是「亲手实现评测体系 + 可量化结论」，不是「做出第二个 Deep Research」。
- **数字必须真实**：所有指标以你的评测集实测为准，不预设、不编造；评测集要可复现。
- 网页抓取遵守目标网站 robots 与服务条款，演示用公开、允许抓取的内容。
- 若 4 周内评测工具没做完，宁可「Agent 简单 + 评测扎实」，也不要「Agent 花哨 + 评测空壳」——评测才是你的亮点。
