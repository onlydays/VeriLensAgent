# VeriLens 前端交接文档（给 kimi 生成前端的完整契约）

> 版本：v1.0　日期：2026-09-07
> 用途：**本文件是前后端对接的唯一事实来源**。后端由 FastAPI 提供（开发中，契约以本文为准）；kimi 只读本文件即可生成前端；无需看后端代码。

---

## 1. 产品一句话（前端要服务的对象）

输入一个研究问题，AI 自动上网搜索、阅读网页、交叉验证，几分钟后返回一份**带来源引用**的调研报告（markdown 文本）。前端就是它的操作界面。

## 2. 交付物形态（硬约束）

- **单个自包含 HTML 文件**（`index.html`），CSS/JS 内联或 CDN 引入，**无需构建工具、无需 npm**。
- 可直接双击 `file://` 打开使用，也可被后端静态托管。
- 界面语言：**简体中文**。风格：简洁、现代、专业（可参考主流 AI 对话产品布局）。
- 技术不限（原生 JS / Vue3 CDN / React CDN 均可），推荐原生 JS + CDN 库（marked.js 渲染 markdown），保证零构建即可运行。

## 3. 联调环境约定

| 项 | 值 |
|---|---|
| 后端地址（默认） | `http://127.0.0.1:8000` |
| 健康检查 | `GET {base}/api/health` → `{"status":"ok"}` |
| CORS | 后端已放开（开发期），`file://` 打开页面可直接调用 |
| 后端启动（Phase 5 后） | `uvicorn src.api.server:app`（届时提供 run.bat） |

高级配置里提供「API 服务地址」输入框（默认填上述地址），可改。

## 4. 页面结构与交互

### 4.1 页面区域（从上到下）

```
┌───────────────────────────────────────────────┐
│ 标题区：VeriLens｜技术调研助手（副标题一句话）    │
├───────────────────────────────────────────────┤
│ 问题输入卡                                    │
│   [ 多行输入框 placeholder="输入你想调研的问题，    │
│     如：调研 Go 与 Rust 在云原生方向的现状对比"  ] │
│   [ 模型配置（可折叠）▲▼ ]                       │
│       API Key:  [password 输入框]（留空用服务端）│
│       Base URL: [http://api.deepseek.com]      │
│       模型:     [deepseek-chat]                │
│       API 地址: [http://127.0.0.1:8000]        │
│       [清除保存的配置]（若用了本地存储）          │
│   [ 🚀 开始调研 ]  [ ⏹ 停止 ]（运行中才可点）    │
├───────────────────────────────────────────────┤
│ 状态区（运行中）：正在搜索…（第 3 步）           │
│ 步骤日志（可选，实时滚动，SSE 联调后用）：         │
│   🔍 调用工具 web_search：…                    │
│   ✅ 工具返回 5 条结果                          │
│   💭 模型决定：继续搜索…                        │
├───────────────────────────────────────────────┤
│ 结果区：渲染返回的 markdown 报告                │
│   [复制报告] [下载 .md]                         │
└───────────────────────────────────────────────┘
```

### 4.2 状态机

| 状态 | 界面表现 |
|---|---|
| idle | 输入框可编辑，「开始调研」可点 |
| running | 两按钮禁用/「开始」变禁用态，输入框只读，显示步骤日志，显示等待提示（**调研可能耗时 1–3 分钟，需明确提示用户勿关闭**） |
| success | 渲染报告；显示"共 X 步 / 用时 Ys"元信息（若有）；恢复 idle |
| error | 顶部/结果区红条显示错误信息（映射表见 §6）；恢复 idle |

### 4.3 文案对照表（kimi 直接用）

| 元素 | 文案 / placeholder |
|---|---|
| 标题 | VeriLens · 技术调研助手 |
| 副标题 | 自动搜索 · 交叉验证 · 每条结论可溯源 |
| 输入框 placeholder | 输入你想调研的问题，如：调研 Go 与 Rust 在云原生方向的现状对比 |
| 配置折叠标题 | 模型配置（一般无需修改） |
| API Key | API Key（选填，留空则用服务端默认） |
| Base URL | Base URL |
| 模型 | 模型 |
| API 地址 | 后端服务地址 |
| 开始按钮 | 🚀 开始调研 |
| 停止按钮 | ⏹ 停止 |
| 运行提示 | 正在调研，可能需要 1–3 分钟，请勿关闭页面 |
| 复制按钮 | 复制报告 |
| 下载按钮 | 下载 .md |

## 5. 同步接口契约（必做：页面主流程走这个）

### POST `{base}/api/research`

**请求头**：`Content-Type: application/json`

**请求体**：
```json
{
  "question": "调研 Go 与 Rust 在云原生方向的现状对比",
  "api_key": "sk-xxxx（选填）",
  "base_url": "https://api.deepseek.com（选填）",
  "model": "deepseek-chat（选填）"
}
```
字段说明：`question` 必填（非空、≤2000 字）；后三个选填，填了覆盖服务端默认（可硬编码在请求里发给后端，后端自行决定用哪个，**前端不做选择逻辑**）。

**成功响应 200**：
```json
{
  "ok": true,
  "data": {
    "report": "## 调研结论\n\n...markdown 正文，引用形如 [1](url)...",
    "sources": ["https://...", "https://..."],
    "steps": 12,
    "duration_ms": 53210,
    "model": "deepseek-chat"
  }
}
```

**失败响应（HTTP 4xx/5xx）**：
```json
{
  "ok": false,
  "error": {
    "code": "LLM_AUTH_FAILED",
    "message": "LLM API Key 无效或未配置，请检查后重试",
    "detail": "可选的技术细节"
  }
}
```

## 6. 错误码 → 前端提示文案映射

前端拿到 `error.code`，展示下表对应文案（拿不到 code 时显示 `message` 本身）：

| code | 前端提示 |
|---|---|
| `MISSING_QUESTION` | 请输入要调研的问题 |
| `NO_LLM_CONFIG` | 未配置模型：请填写 API Key / Base URL / 模型，或让服务端配置 .env |
| `LLM_AUTH_FAILED` | API Key 无效或无权限，请检查 API Key |
| `LLM_QUOTA` | 额度不足或触发限流，请检查账户余额，稍后重试 |
| `UPSTREAM_ERROR` | 上游服务（大模型/搜索）暂不可用，请稍后重试 |
| `TOO_MANY_STEPS` | 调研步数超限已提前结束，结论可能不完整，建议人工复核 |
| `INTERNAL` | 服务内部错误，请查看后端日志 |

## 7. 流式接口契约（可选增强：实时步骤。后端 Phase 6 提供前，前端只实现 UI 占位即可）

### POST `{base}/api/research/stream`

请求体与 §5 相同。响应为 `text/event-stream`，每个事件两行（`event:` + `data:`），`data` 为 JSON：

| event | data 示例 | 前端动作 |
|---|---|---|
| `start` | `{"question":"..."}` | 显示"开始调研" |
| `step` | `{"index":1,"kind":"reasoning","content":"模型决定先搜索..."}` | 追加 💭 日志 |
| `tool_call` | `{"index":2,"name":"web_search","arguments":{"query":"..."}}` | 追加 🔍 日志 |
| `tool_result` | `{"index":3,"name":"web_search","ok":true,"preview":"返回5条结果…","error":null}` | 追加 ✅/⚠️ 日志 |
| `final` | `{"report":"## 结论…","sources":["https://…"],"steps":12,"duration_ms":53210}` | 渲染报告，状态→success |
| `error` | `{"code":"LLM_AUTH_FAILED","message":"…"}` | 按 §6 提示，状态→error |
| `done` | `{"reason":"finished"}` | 结束 |

### SSE 消费参考代码（可直接使用或改写）

```js
async function researchStream(body, onEvent) {
  const res = await fetch(`${apiBase}/api/research/stream`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify(body),
  });
  if (!res.ok) { /* 读 JSON 错误体 → §6 提示 */ return; }
  const reader = res.body.getReader();
  const dec = new TextDecoder();
  let buf = "";
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    buf += dec.decode(value, { stream: true });
    const parts = buf.split("\n\n");
    buf = parts.pop();
    for (const part of parts) {
      const evLine = part.split("\n").find(l => l.startsWith("event:"));
      const dataLine = part.split("\n").find(l => l.startsWith("data:"));
      if (!evLine || !dataLine) continue;
      const ev = evLine.slice(6).trim();
      const data = JSON.parse(dataLine.slice(5).trim());
      onEvent(ev, data);
    }
  }
}
```

## 8. 结果渲染约定

- 报告的 `report` 字段是 **markdown 文本**，用 CDN 引入 marked.js 渲染成 HTML（`https://cdnjs.cloudflare.com/ajax/libs/marked/…/marked.min.js`，固定版本号）。
- **安全**：渲染后禁止再执行其中任何脚本/HTML（marked 默认不执行脚本；不要再拼 innerHTML 执行）；所有链接 `target="_blank" rel="noopener"`。
- 报告中引用链接形如 `[1](https://…)`，必须**可点击、可识别目的地**（不吞掉 URL）。
- 报告长时结果区可滚动，不要撑破页面；`word-break` 防长 URL 溢出。

## 9. 本地存储（可选，推荐）

- 用 `localStorage` 记住用户填的 `api_key / base_url / model / api 地址`，下次打开自动回填；提供「清除保存的配置」按钮。
- **安全提示（可在配置区小字展示）**：本项目为本地工具，Key 仅发送到你填写的本地后端；请勿在公用电脑保存 Key。

## 10. 验收清单（交付前逐条自检 + 用户最终人工测试用）

1. `file://` 双击打开无报错，界面中文、布局正常。
2. 不填问题点「开始调研」→ 提示"请输入要调研的问题"。
3. 填问题、点开始 → 按钮禁用、显示运行提示。
4. 后端未启动时请求 → 显示"无法连接后端服务"类错误（fetch 网络错误要 catch 并提示，不能白屏）。
5. 填错 API Key → 显示"API Key 无效"提示（映射表文案）。
6. 配置正确 → 1–3 分钟后渲染出 markdown 报告，引用链接可点击新窗口打开。
7. 「复制报告」复制的是纯 markdown 文本；「下载 .md」能下载到文件。
8. 关闭页面/停止按钮可中断请求（AbortController）。
9. 刷新页面后配置回填（若用了 localStorage）。
10. 手机/窄窗口下布局不错乱（非硬性，尽力即可）。

## 11. 禁止事项（kimi 与后端各自的边界）

- 前端**不得**伪造/模拟报告数据；所有数据只来自后端接口。
- 前端**不得**自行调用任何 LLM/搜索 API——一切请求走后端 `/api/research`。
- 前端**不实现**业务逻辑（如何调研、如何评测），只负责收集输入、调接口、渲染结果、管理状态。
- 不要把 API Key 发送到除用户填写的后端地址以外的任何地方。
